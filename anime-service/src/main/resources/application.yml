spring:
  application:
    name: anime-service
  threads:
    virtual:
      enabled: true

logging:
  level:
    com.dev.fellipe.anime_service: debug

server:
  servlet:
    context-path: /
  error:
    include-stacktrace: on_param
  tomcat:
    threads:
      max: 20